/**
 * Enumeração que define chaves para acessar dados no localStorage.
 * Esta enumeração fornece chaves predefinidas para diferentes tipos de dados armazenados localmente.
 */
export enum LocalStorageEnum {
  /**
   * Chave para acessar os dados do usuário autenticado.
   */
  AuthUser = 'auth-user',
}
